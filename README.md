# 📝 Django Todo Application

A simple yet powerful **Todo Application** built using **Django** with user authentication and CRUD functionality.  
Users can add, update, and delete their own tasks securely.

---

## 🚀 Features

- User Authentication (Login / Logout)
- Add new tasks
- Update existing tasks (inline editing)
- Delete tasks
- User-specific todos (each user sees only their tasks)
- Clean & responsive UI
- Django ORM for database operations
- Ready for API integration (DRF compatible)

---

## 🛠️ Tech Stack

- **Backend:** Django
- **Database:** SQLite (can be switched to PostgreSQL)
- **Frontend:** HTML, CSS
- **Authentication:** Django Auth
- **ORM:** Django ORM



